{
  "type": "component",
  "metadata": {},
  "schema": {
    "name": "scatter-plot",
    "friendlyName": "scatter-plot",
    "description": "An amazing Budibase component!",
    "icon": "Text",

    "settings": [
      {
        "type": "dataProvider",
        "label": "Data provider",
        "key": "dataProvider",
        "required": true
      },
      {
        "type": "text",
        "label": "Title for the chart",
        "key": "title"
      },

      {
        "type": "field",
        "label": "X axis column",
        "key": "xaxis",
        "required": true
      },
      {
        "type": "field",
        "label": "time column",
        "key": "tm",
        "required": true
      },
      {
        "type": "field",
        "label": "Y axis column",
        "key": "yaxis",
        "required": true
      }
    ]
  }
}
